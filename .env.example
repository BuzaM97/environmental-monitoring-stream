# InfluxDB credentials (change these in .env)
INFLUXDB_ADMIN_PASSWORD=changeme
INFLUXDB_INIT_PASSWORD=changeme
INFLUXDB_INIT_ADMIN_TOKEN=changeme_generate_strong_token

# Grafana credentials (change these in .env)
GRAFANA_ADMIN_PASSWORD=changeme
